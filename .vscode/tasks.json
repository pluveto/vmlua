{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build Project for Debug",
            "command": "${workspaceFolder}/scripts/build.sh",
            "group": {
                "kind": "build"
            }
        }
    ]
}